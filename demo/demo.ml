let () = Printf.printf "General.Int.zero: %n\n" General.Int.zero

let () = Printf.printf "General.Int.one: %n\n" General.Int.one

let () = Printf.printf "General.Int.(one = zero): %b\n" General.Int.(one = zero)
